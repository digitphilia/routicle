# -*- encoding: utf-8 -*-

"""
Core Functionalities on a NetworkX Graph

The module is dependend on :mod:`networkx` library and solves the
problem by using the graph methods. The module is exposed with the
same acronym ``nx`` as the parent module import.

.. code-block:: python

    import routicle.nx as ronx
    ...

The module is implemented on an OOP approach, and all the base method
and functions are defined under the ``base.py`` file.
"""

